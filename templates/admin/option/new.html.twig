{% extends 'base.html.twig' %}
{% block title 'Ajouter une nouvelle' %}

{% block body %}
    <div class="container mt-4">
        <h1>Ajouter une nouvelle option</h1>
            {{ form_start(form) }}
            {{ form_rest(form) }}
            <button class="btn btn-primary">Ajouter</button>
            {{ form_end(form) }}
    </div>
{% endblock %}